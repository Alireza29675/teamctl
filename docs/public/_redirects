# Cloudflare Pages redirects (https://developers.cloudflare.com/pages/configuration/redirects/)
#
# /install used to 302 to GitHub raw, but cargo-dist's release-publish window
# could briefly serve odd content through that redirect chain (msgs 2244 +
# 2474 + 2636). install.sh now ships as a static asset under docs/public/install
# so Cloudflare Pages serves it directly with no redirect dependency.
#
# Single source of truth: tools/install.sh. docs/public/install is a synced
# copy regenerated by `just sync-install` (or manually) before each release.
