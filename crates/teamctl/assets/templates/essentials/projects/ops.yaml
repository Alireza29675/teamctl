# projects/ops.yaml — the helper team.
#
# One agent, `builder`, whose job is to scaffold and evolve `main`
# for you. Talk to it on Telegram once you've wired the bot token
# (see `.env.example`).
#
# Don't edit this file unless you know what you're doing — reshaping
# the ops project can leave you without a helper.

version: 2

project:
  id: ops
  name: Ops
  cwd: ./workspace

channels:
  - name: all
    members: "*"

managers:
  builder:
    runtime: claude-code
    model: claude-opus-4-7
    role_prompt: roles/builder.md
    # `auto` lets the builder run tools without per-action HITL;
    # `low_risk_only` keeps destructive operations (rm, force, push)
    # behind a `request_approval` gate, which lands in your Telegram
    # chat with the builder.
    permission_mode: auto
    autonomy: low_risk_only
    can_dm: []
    can_broadcast: [all]
    interfaces:
      telegram:
        bot_token_env: TEAMCTL_TG_BUILDER_TOKEN
        chat_ids_env: TEAMCTL_TG_BUILDER_CHATS

workers: {}
