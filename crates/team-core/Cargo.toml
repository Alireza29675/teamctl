[package]
name = "team-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared library for teamctl: YAML schema, validation, and artifact rendering."

[dependencies]
anyhow.workspace = true
thiserror.workspace = true
serde.workspace = true
serde_yaml.workspace = true
serde_json.workspace = true
yaml-edit.workspace = true
schemars.workspace = true
semver.workspace = true
tracing.workspace = true
rusqlite.workspace = true
blake3.workspace = true
chrono = { version = "0.4", default-features = false, features = ["clock"] }
uuid.workspace = true

[dev-dependencies]
tempfile = "3"
# #347: capture the RO-FS legacy-`2` rewrite degrade's `tracing::warn` in a
# buffer so the test can assert the degrade is observable, not a silent
# swallow. Test-only; the shipped lib never builds a subscriber.
tracing-subscriber.workspace = true
