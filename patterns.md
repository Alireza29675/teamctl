# Patterns

Captures coding and repo patterns this project follows. Accretes as the team learns; review when refactoring so the patterns stay current with the code.

Each pattern: short name, the rule, one example or pointer. If a pattern is no longer load-bearing, delete it rather than letting it rot.
